# requirements

Offline backup of FR/NFR requirements exported from Notion. Notion remains the source of truth — this folder only holds manual export snapshots (Markdown or PDF), left empty until exported.

Naming convention: `YYYY-MM-DD-<export-description>.md` (or `.pdf`).
