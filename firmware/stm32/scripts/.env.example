# Copy this file to .env and fill in real values. .env is gitignored.

RPI_HOST=raspberrypi.local
RPI_USER=pi
RPI_PORT=22
RPI_REMOTE_DIR=/tmp/stm32-firmware
PIO_ENV=bluepill_f103c8
FLASH_ADDR=0x08000000
# deploy_via_rpi.bat only (Windows) — path to pio.exe, since it's not on PATH by default.
PIO_BIN=C:\Users\trand\.platformio\penv\Scripts\pio.exe
